#ifndef INT_TAPE_DELAY_PARSE
#define INT_TAPE_DELAY_PARSE

#include "../TapeDelays/TapeDelays.h"
#include <fstream>
#include <iterator>
#include <sstream>
#include <vector>

TapeDelays delayParse(const char *file_name);

#endif // INT_TAPE_DELAY_PARSE
